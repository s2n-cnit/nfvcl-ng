[pytest]
log_cli = true
testpaths = tests
markers =
    integration: tests that require NFVCL services or deployed infrastructure
    provider_vim: direct provider tests that connect to real VIMs from provider test config
    provider_destructive: direct provider tests that create/delete real VIM resources
