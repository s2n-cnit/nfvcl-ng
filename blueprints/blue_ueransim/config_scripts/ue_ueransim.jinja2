# IMSI number of the UE. IMSI = [MCC|MNC|MSISDN] (In total 15 or 16 digits)
supi: 'imsi-{{ confvar.sim.imsi }}'
# Mobile Country Code value of HPLMN
mcc: '{{ confvar.sim.plmn[:3] }}'
# Mobile Network Code value of HPLMN (2 or 3 digits)
mnc: '{{ confvar.sim.plmn[3:] }}'

# Permanent subscription key
key: '{{ confvar.sim.key }}'
# Operator code (OP or OPC) of the UE
op: '{{ confvar.sim.op }}'
# This value specifies the OP type and it can be either 'OP' or 'OPC'
opType: '{{ confvar.sim.opType }}'
# Authentication Management Field (AMF) value
amf: '{{ confvar.sim.amf }}'
# IMEI number of the device. It is used if no SUPI is provided
imei: '356938035643803'
# IMEISV number of the device. It is used if no SUPI and IMEI is provided
imeiSv: '4370816125816151'

# List of gNB IP addresses for Radio Link Simulation
gnbSearchList:
{{ confvar.gnbSearchList | to_yaml(indent=2, default_flow_style=False) }}

# UAC Access Identities Configuration
uacAic:
  mps: false
  mcs: false

# UAC Access Control Class
uacAcc:
  normalClass: 0
  class11: false
  class12: false
  class13: false
  class14: false
  class15: false

# Initial PDU sessions to be established
sessions:
{{ confvar.sim.sessions | to_yaml(indent=2, default_flow_style=False) }}

# Configured NSSAI for this UE by HPLMN
configured-nssai:
{{ confvar.sim.configured_nssai | to_yaml(indent=2, default_flow_style=False) }}

# Default Configured NSSAI for this UE
default-nssai:
{{ confvar.sim.default_nssai | to_yaml(indent=2, default_flow_style=False) }}

# Supported encryption algorithms by this UE
integrity:
  IA1: true
  IA2: true
  IA3: true

# Supported integrity algorithms by this UE
ciphering:
  EA1: true
  EA2: true
  EA3: true

# Integrity protection maximum data rate for user plane
integrityMaxRate:
  uplink: 'full'
  downlink: 'full'